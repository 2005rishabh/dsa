## Queue – Java

### Declaration
Queue<Integer> q = new LinkedList<>();

### Core Methods
add(x)
offer(x)
poll()
remove()
peek()
element()
isEmpty()
size()

### Notes
- FIFO
- `poll()` returns null if empty
- `remove()` throws exception if empty
